
= linux

[source,shell]
----
docker run \
  -it \
  -e DEBIAN_FRONTEND=noninteractive \
  -e TZ=Asia/Shanghai \
  -v ~/dl:/data \
  -w /data \
  ubuntu \
  /bin/bash

apt update -y \
  && apt upgrade -y

----

查看版本
[source,shell script]
----

uname -a

cat /etc/centos-release
cat /etc/redhat-release

cat /etc/os-release

----
