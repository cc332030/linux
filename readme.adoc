
= linux

[source,shell]
----
docker run \
  -it --rm \
  -v .:/data \
  -w /data \
  ubuntu \
  /bin/bash
----

查看版本
[source,shell script]
----

uname -a

cat /etc/centos-release
cat /etc/redhat-release

cat /etc/os-release

----
